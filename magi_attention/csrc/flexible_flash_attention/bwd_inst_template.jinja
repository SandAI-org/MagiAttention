// Auto-generated minimal backward instantiation module (JIT).
// Single-variant compilation bound to URI parameters: arch, compute dtype, DKV output dtype, head dim, softcap, and dkv-atomic options.

#include <cstdlib>
#include <torch/python.h>

#include "flex_flash_common.hpp"
#include "flash_bwd_launch_template.h"
#include "flex_flash_bwd.hpp"
#include "extensions/profile_utils.h"

// Compile-time constants provided by Jinja arguments
static constexpr int kArch = {{ arch_sm_num }};
using TCompute = {{ compute_t }};
using TDq      = {{ dq_t }};
using TDkv     = {{ dkv_t }};
static constexpr int kHeadDim = {{ head_dim }};
static constexpr bool kHasSoftcap = {{ has_softcap }};
static constexpr bool kDisableDkvAtomic = {{ disable_atomic }};
static constexpr bool kDeterministic = {{ deterministic }};
static constexpr bool kProfileMode = {{ profile_mode }};
static constexpr bool kRangeMerge = {{ auto_range_merge }};
static constexpr bool kSwapBwdQKLoop = {{ swap_bwd_qk_loop }};
static constexpr bool kPackGQA = {{ pack_gqa }};
static constexpr bool kCatGQA = {{ cat_gqa }};
static constexpr int kQheadPerKhead = {{ qhead_per_khead }};
static constexpr bool kBlockSparse = {{ block_sparse }};
static constexpr bool kIndexSparse = {{ index_sparse }};
static constexpr bool kUseMaskDispatch = {{ use_mask_dispatch | default('true') }};
static constexpr bool kInnerDirMaxToMin = {{ inner_dir_max_to_min | default('false') }};
// mask_mode: 0=regular(direct apply every block), 1=dispatch(3-lambda zone split), 2=unified(runtime zone dispatch)
static constexpr int kMaskMode = {{ mask_mode_int | default('1') }};
static constexpr bool kInnerDxStoreInProducer = {{ inner_dx_store_in_producer | default('true') }};
// setmaxnreg quotas for the Load/Mma WGs, selected by _ffa_register_quota on the Python
// side (env override: MAGI_ATTENTION_FFA_BWD_PRODUCER_REGS); the kernel only asserts them.
static constexpr int kBwdProducerRegs = {{ bwd_producer_regs }};
static constexpr int kBwdConsumerRegs = {{ bwd_consumer_regs }};
// Inner-loop scatter load mode: 0=Tma(2D,auto), 2=CpAsync(per-row)
static constexpr int kInnerLoadMode = {{ inner_load_mode | default(2) }};
// Inner-loop scatter store mode: 1=Tma1d(bulk reduce-add), 2=CpAsync(scalar atomicAdd)
static constexpr int kInnerStoreMode = {{ inner_store_mode | default(1) }};
// Tile/stage overrides for A/B benchmarking (env: MAGI_ATTENTION_FFA_BWD_TILE_M/N, MAGI_ATTENTION_FFA_BWD_STAGES/DS/V).
// 0 = use default from tile_size_bwd_sm90(); >0 = override.
static constexpr int kBwdTileM = {{ bwd_tile_m | default(0) }};
static constexpr int kBwdTileN = {{ bwd_tile_n | default(0) }};
static constexpr int kBwdStages = {{ bwd_stages | default(0) }};
static constexpr int kBwdStagesDs = {{ bwd_stages_ds | default(0) }};
static constexpr int kBwdStagesV = {{ bwd_stages_v | default(0) }};
// SMEM-saving overrides (env: MAGI_ATTENTION_FFA_BWD_SCATTER_PAD, MAGI_ATTENTION_FFA_BWD_LSE_UNION).
// scatter_pad: -1 = auto (4 for kBlockN<=64, 0 for kBlockN>=128), >=0 = override.
// lse_union: 0 = separate smem_lse/smem_dpsum, 1 = union with dKVacc buffer.
static constexpr int kBwdScatterPad = {{ bwd_scatter_pad | default(-1) }};
static constexpr int kBwdLseUnion = {{ bwd_lse_union | default(0) }};
// dKVacc bypass: 0 = normal SMEM roundtrip, 1 = consumer register atomicAdd (bypass SMEM)
static constexpr int kBwdDkvaccBypass = {{ bwd_dkvacc_bypass | default(0) }};
// IndexSparse K block size: 1 = token-level indices, >1 = block-level (each index covers KBlockSize tokens)
static constexpr int kKBlockSize = {{ k_block_size | default(1) }};
// Force Mma_dKV to SS mode (SMEM-SMEM) instead of RS for benchmarking register pressure.
static constexpr bool kForceMmaDkvSS = {{ force_mma_dkv_ss | default('false') }};

static_assert(!kSwapBwdQKLoop || !kDeterministic, "Deterministic mode is not supported when SwapBwdQKLoop is enabled.");
static_assert(!(kBlockSparse && kIndexSparse), "BlockSparse and IndexSparse are mutually exclusive.");
// Runtime contract checks to ensure consistency with compile-time constraints
static inline void _check_runtime_contract_bwd(
    const at::Tensor& q,
    std::optional<at::ScalarType> dk_type_opt,
    std::optional<at::ScalarType> dv_type_opt,
    float softcap) {
  TORCH_CHECK(q.scalar_type() == at::kHalf || q.scalar_type() == at::kBFloat16,
              "JIT-FFA bwd only supports fp16/bf16 compute");
  TORCH_CHECK(q.size(2) == kHeadDim,
              "HeadDim mismatch: compiled=", kHeadDim, ", runtime=", q.size(2));
  TORCH_CHECK(((softcap > 0.f) == kHasSoftcap),
              "softcap mismatch with compiled variant");

  auto check_dkv = [](std::optional<at::ScalarType> t, const char* name) {
    if (!t.has_value()) return;
    at::ScalarType ty = t.value();
    if constexpr (std::is_same_v<TDkv, float>) {
      TORCH_CHECK(ty == at::kFloat, name, " must be float32 for this JIT variant");
    } else if constexpr (std::is_same_v<TDkv, cutlass::half_t>) {
      TORCH_CHECK(ty == at::kHalf, name, " must be float16 for this JIT variant");
    } else if constexpr (std::is_same_v<TDkv, cutlass::bfloat16_t>) {
      TORCH_CHECK(ty == at::kBFloat16, name, " must be bfloat16 for this JIT variant");
    }
  };
  check_dkv(dk_type_opt, "dk_type");
  check_dkv(dv_type_opt, "dv_type");
}

// Backward implementation: matches the binding signature
std::vector<at::Tensor> mha_bwd(
    const at::Tensor& dout,
    const at::Tensor& q,
    const at::Tensor& k,
    const at::Tensor& v,
    const std::optional<at::Tensor>& sink_,
    const at::Tensor& out,
    std::optional<const at::Tensor>& dq_,
    std::optional<const at::Tensor>& dk_,
    std::optional<const at::Tensor>& dv_,
    std::optional<const at::Tensor>& dsink_,
    const at::Tensor& softmax_lse,
    std::optional<const at::Tensor>& q_ranges_,
    std::optional<const at::Tensor>& k_ranges_,
    std::optional<const at::Tensor>& attn_type_map_,
    std::optional<const at::Tensor>& merge_k_ranges_,
    std::optional<const at::Tensor>& bwd_kq_map_,
    std::optional<const at::Tensor>& bwd_unique_count_,
    std::optional<const at::Tensor>& index_sparse_indices_,
    int index_sparse_max_topk,
    int index_sparse_k_block_size,
    float const softmax_scale,
    float const softcap,
    std::optional<at::ScalarType> dq_type_,
    std::optional<at::ScalarType> dk_type_,
    std::optional<at::ScalarType> dv_type_,
    const std::string& sink_layout_,
    int const sm_margin) {
  _check_runtime_contract_bwd(q, dk_type_, dv_type_, softcap);

  auto stream = at::cuda::getCurrentCUDAStream().stream();

  // Parameter preparation (including output tensor allocation)
  auto [params, dq, dk, dv, dsink] = prepare_mha_bwd<kDeterministic, kDisableDkvAtomic, kSwapBwdQKLoop, kPackGQA, kCatGQA, kIndexSparse>(
      dout, q, k, v, sink_, out, dq_, dk_, dv_, dsink_, softmax_lse, 
      q_ranges_, k_ranges_, attn_type_map_, 
      merge_k_ranges_, bwd_kq_map_, bwd_unique_count_, 
      index_sparse_indices_, index_sparse_max_topk, index_sparse_k_block_size,
      softmax_scale, softcap,
      dq_type_, dk_type_, dv_type_, 
      sink_layout_, sm_margin);

  // dKV pool: split dK/dV accum into pool_count slices to reduce L2 contention
  int pool_count = 1;
  at::Tensor dk_pool, dv_pool;
  const char* pool_env = std::getenv("MAGI_ATTENTION_FFA_BWD_DKV_POOL_COUNT");
  if (pool_env) pool_count = std::max(1, std::atoi(pool_env));
  if (pool_count > 1 && kSwapBwdQKLoop) {
    auto opts = dk.options().dtype(torch::kFloat);
    int total_k = params.total_k;
    dk_pool = torch::zeros({(int64_t)pool_count * total_k, params.h_kv, params.d}, opts);
    dv_pool = torch::zeros({(int64_t)pool_count * total_k, params.h_kv, params.d}, opts);
    params.dk_ptr = dk_pool.data_ptr();
    params.dv_ptr = dv_pool.data_ptr();
    params.dk_row_stride = dk_pool.stride(0);
    params.dk_head_stride = dk_pool.stride(1);
    params.dv_row_stride = dv_pool.stride(0);
    params.dv_head_stride = dv_pool.stride(1);
    params.pool_count = pool_count;
    params.pool_seqlen_k = total_k;
  }
  
  if constexpr (kProfileMode)
    MagiEvents::stop("bwd_prepare");
  
  // Kernel launch (single variant)
  run_mha_bwd_<kArch, TCompute, TDq, TDkv, kHeadDim, kHasSoftcap, kDisableDkvAtomic, kDeterministic, kRangeMerge, kSwapBwdQKLoop, kPackGQA, kCatGQA, kQheadPerKhead, kBlockSparse, kIndexSparse, kUseMaskDispatch, kInnerDirMaxToMin, kMaskMode, kInnerDxStoreInProducer, kBwdProducerRegs, kBwdConsumerRegs, kInnerStoreMode, kBwdTileM, kBwdTileN, kBwdStages, kBwdStagesDs, kBwdStagesV, kBwdScatterPad, kBwdLseUnion, kBwdDkvaccBypass, kKBlockSize, kForceMmaDkvSS, kInnerLoadMode, kProfileMode>(params, stream);

  // Pool reduction: sum pool slices and convert to output dtype
  if (pool_count > 1 && kSwapBwdQKLoop) {
    int total_k = params.pool_seqlen_k;
    auto dk_summed = dk_pool.view({pool_count, total_k, params.h_kv, params.d}).sum(0);
    auto dv_summed = dv_pool.view({pool_count, total_k, params.h_kv, params.d}).sum(0);
    dk.copy_(dk_summed.to(dk.dtype()));
    dv.copy_(dv_summed.to(dv.dtype()));
  }

  return {dq, dk, dv, dsink};
}

PYBIND11_MODULE(TORCH_EXTENSION_NAME, m) {
  m.def("bwd", &mha_bwd, "Backward (single-variant JIT)");
}
